server:
  port: 8084
  
spring:
  datasource:
   url: jdbc:mysql://localhost:3306/petroldb
   username: root
   password: 1234
  jpa:
    hibernate:
      ddl-auto: update
      
spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.MySQLDialect